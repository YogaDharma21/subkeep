version: '3.8'

services:
  # SubKeep Web Application
  website:
    build:
      context: ../apps/website
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
